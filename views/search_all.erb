<!doctype html>
<html lang="en">
  <head>
    <link rel="stylesheet" type="text/css" href="/css/base.css">
    <title>Sedgewick 的飯否</title>
    <style>
      body {
        padding: 10px;
        font-size: 14px;
      }

      h1 {
        font-size: 16px;
		color: white;
      }

      h2 {
        font-size: 20px;
        text-decoration: underline;
      }

      h3 {
        font-size: 20px;
      }

      section > header {
        margin-bottom: 10px;
      }

      section > header > h2 {
        font-size: 20px;
      }

      ul {
        list-style-position: inside;
        margin: 10px 0;
	    background-color: #fafafa;
	    border: 1px solid #ccc;
	    border-radius: 4px;
	    padding: 10px;
      }
	  
      li {
        list-style-position: inside;
        margin: 5px 0;
      }
	  
	  li.slice {
		background-color: #ececec;
		padding: 5px;
		border-radius: 4px;
	  }
	  
	  ul a {
	    color: #999;
        font-size: smaller;
	  }

      section p {
        margin: 15px 0 30px 0;
      }

      ul:hover {
        background-color: #fbf9e6;
      }
	  
	  .tag {
  	    background-color: #fcf284;
  		display: inline-block;
  		border-radius: 4px;
  		padding: 0px 10px;
  		box-shadow: 1px 1px 3px #9b8e02; 
	  }
	  
	  
	  
	  
	  
      body {
          background: #555 url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAUAAAAFCAYAAACNbyblAAAAGXRFWHRTb2Z0d2FyZQBBZG9iZSBJbWFnZVJlYWR5ccllPAAAAB9JREFUeNpi/P//PwM6YGLAAuCCmpqacC2MRGsHCDAA+fIHfeQbO8kAAAAASUVORK5CYII=);
      }
    
      a {
          color: #ccc;
      }
    
      /*-------------------------------------*/
    
      .cf:before, .cf:after{
        content:"";
        display:table;
      }
    
      .cf:after{
        clear:both;
      }

      .cf{
        zoom:1;
      }

      /*-------------------------------------*/	
    
      .form-wrapper {
          width: 600px;
          padding: 15px;
          margin: 150px auto 30px auto;
          background: #444;
          background: rgba(0,0,0,.2);
          -moz-border-radius: 10px;
          -webkit-border-radius: 10px;
          border-radius: 10px;
          -moz-box-shadow: 0 1px 1px rgba(0,0,0,.4) inset, 0 1px 0 rgba(255,255,255,.2);
          -webkit-box-shadow: 0 1px 1px rgba(0,0,0,.4) inset, 0 1px 0 rgba(255,255,255,.2);
          box-shadow: 0 1px 1px rgba(0,0,0,.4) inset, 0 1px 0 rgba(255,255,255,.2);
      }
    
      .form-wrapper input {
          width: 480px;
          height: 20px;
          padding: 10px 5px;
          float: left;    
          font: bold 15px 'lucida sans', 'trebuchet MS', 'Tahoma';
          border: 0;
          background: #eee;
          -moz-border-radius: 3px 0 0 3px;
          -webkit-border-radius: 3px 0 0 3px;
          border-radius: 3px 0 0 3px;      
      }
    
      .form-wrapper input:focus {
          outline: 0;
          background: #fff;
          -moz-box-shadow: 0 0 2px rgba(0,0,0,.8) inset;
          -webkit-box-shadow: 0 0 2px rgba(0,0,0,.8) inset;
          box-shadow: 0 0 2px rgba(0,0,0,.8) inset;
      }
    
      .form-wrapper input::-webkit-input-placeholder {
         color: #999;
         font-weight: normal;
      }
    
      .form-wrapper input:-moz-placeholder {
          color: #999;
          font-weight: normal;
      }
    
      .form-wrapper input:-ms-input-placeholder {
          color: #999;
          font-weight: normal;
      }    
    
      .form-wrapper button {
  		overflow: visible;
          position: relative;
          float: right;
          border: 0;
          padding: 0;
          cursor: pointer;
          height: 40px;
          width: 110px;
          font: bold 15px/40px 'lucida sans', 'trebuchet MS', 'Tahoma';
          color: #fff;
          text-transform: uppercase;
          background: #d83c3c;
          -moz-border-radius: 0 3px 3px 0;
          -webkit-border-radius: 0 3px 3px 0;
          border-radius: 0 3px 3px 0;      
          text-shadow: 0 -1px 0 rgba(0, 0 ,0, .3);
      }   
      
      .form-wrapper button:hover{		
          background: #e54040;
      }	
      
      .form-wrapper button:active,
      .form-wrapper button:focus{   
          background: #c42f2f;    
      }
    
      .form-wrapper button:before {
          content: '';
          position: absolute;
          border-width: 8px 8px 8px 0;
          border-style: solid solid solid none;
          border-color: transparent #d83c3c transparent;
          top: 12px;
          left: -6px;
      }
    
      .form-wrapper button:hover:before{
          border-right-color: #e54040;
      }
    
      .form-wrapper button:focus:before{
          border-right-color: #c42f2f;
      }    
    
      .form-wrapper button::-moz-focus-inner {
          border: 0;
          padding: 0;
      }
	  
	  
	  .tip {
		  font-size: 12px;
		  color: #908f8f;
		  margin: 10px auto;
		  width: 300px;
	  }
    </style>
  </head>
  <body>
    <header>
      <h1><a href="/">飯糰🍙</a> / 「<%= @word %>」</h1>
    </header>
	<form class="form-wrapper cf" action="/search_all">
		<input type="text" placeholder="試試「@[^S].*?(?=[，。（）「」\s])」或者「::people::」" name="word" required>
		<button type="submit">Search</button>
	</form>

<%
if @word.nil? %>
	<div class="tip" style="text-align: left">支持定製搜索：
		<br />
		- 關鍵詞前加「fanfou:」將搜索範圍限定在飯否；
		<br />
		- 關鍵詞前加「blog:」將搜索範圍限定在飯否；
		<br />
		- 關鍵詞前加「stat:」分類統計搜索結果；
		<br />
		- 關鍵詞前加「map:」給出搜索詞的熱力圖；
		<br />
		- 關鍵詞前加「code:」搜索代碼庫；
		<br />
		- 關鍵詞前加「date:」搜索該天發佈的飯嘮或文章；
	</div>
<% end
%>
	
	
<%
def redirect_to(lable, to, query)
    pat = Regexp.new("^#{lable}:\s*(.+)", Regexp::IGNORECASE)
    redirect to("#{to}?word=#{CGI.escape(query.scan(pat).first.first)}") if pat.match(query)
end

unless @word.nil?
	pattern = CGI.unescape(@word.gsub('+', '::plus::')).gsub(/\s/, '').gsub('::plus::', '+')
	puts "word=#{@word}"
	puts "search => #{pattern}"
	
	redirect_to('fanfou', '/search', pattern)
	redirect_to('blog', '/search_blog', pattern)
	redirect_to('stat', '/chart', pattern)
	redirect_to('map', '/stat', pattern)
	redirect_to('code', '/search_code', pattern)
	redirect_to('date', '/search_all_by_time', pattern)
	
	pattern = /@[^S].*?(?=[，。（）「」\s])/ if @word == "::people::"
	redirect '/niu' if @word == "妞妞"
	redirect '/nimr' if @word == "妮妹"
	redirect '/ideas' if @word == "創意"
	
	
	# search fanfou
	
	targets = []
	
	@conversations.each do |items|
	  content = ""
	  items.each do |k|
	    content += @statuses_pool[k]["text"]
	  end

	  targets << items if Regexp.new("#{pattern}", Regexp::IGNORECASE).match(content)
	end
	
	
	# search JianShu
	
	s = File.open("./data/articles.json", "r").read
	        .gsub("=>", ":")
	        .gsub(/\\u\{(\w{5})\}/) { [$1.to_i(16)].pack("U") }

	blogs = JSON.parse(s)
	
	matched_blogs = []
	
	blogs.each do |date, h|
	  if h['html'].index Regexp.new("#{pattern}", Regexp::IGNORECASE)
	    matched_blogs << {
						    :url => h['url'],
						    :title => h['title'],
						    :date => date,
						    :slices => h['html'].scan(Regexp.new("(<p>.*(#{pattern}).*</p>)", Regexp::IGNORECASE)).reduce([]) { |memo, e| memo << e.first }
				         }
	  end
	end
	# pp matched_blogs
	
%>
	
	
	<h1 style="text-align: center">
		
		<%
		  puts "fanfou: #{targets.size}"
		  puts "blog: #{matched_blogs.size}"
		  if targets.size > 0 and matched_blogs.size > 0
		    %>
			關於「<%= @word%>」，找到了 <%= targets.size %> 段飯嘮 & <%= matched_blogs.size %> 篇文章😏
			<%
		  elsif targets.size == 0 and matched_blogs.size > 0
		    %>
			沒有任何關於「<%= @word %>」的飯嘮，但，找到了 <%= matched_blogs.size %> 篇文章😏
			<%
		  elsif targets.size > 0 and matched_blogs.size == 0
		    %>
			沒有任何關於「<%= @word %>」的文章，但，找到了 <%= targets.size %> 段飯嘮😏
			<%
		  else
		    %>
			沒有任何關於「<%= @word %>」的發現☹️
			<br />
			<% if @word.include? '+' %>
			  <div class="tip">看看是不是沒有把「+」轉義為「\+」？</div>
			<% elsif @word.include? ' ' %>
			  <div class="tip">看看是不是沒有把「space」轉義為「\s」？</div>
			<%
			end
		  end
		%>
	</h1>
	
    <section>
      <br /> <br />

<% targets.each do |items| %>
	  <ul class="fanfou">
  
	  <% items.each do |k| %>

		<li>
	      <a target="_blank" href="http://fanfou.com/statuses/<%= k %>?fr=viewreply"><%= Time.parse(@statuses_pool[k]["created_at"]).to_s[0..-7] %></a>
		  - <%= @statuses_pool[k]["text"].gsub(/\n/, "<br />").gsub(/(#{pattern})/i, '<div class="tag">\1</div>') %>
		  <br />
		  <% unless @statuses_pool[k]["photo_url"].nil? %>
		    <a href="<%= @statuses_pool[k]["photo_url"] %>">
		      <img src="<%= @statuses_pool[k]["photo_url"] %>">
		    </a>
		  <% end %>
	    </li>

	  <% end %>

	  </ul>

	<% end
	
	
	
    matched_blogs.each do |h| %>
	  	<ul class="blog">
		  	  <a href="<%= h[:url] %>"><%= h[:title] + ' ' + h[:date] %></a>
			  <%
			  ary = []
			  h[:slices].each do |s|
			    ary << '<li class="slice">' + s.to_markdown.gsub(/(#{pattern})/i, '<div class="tag">\1</div>') + '</li>'
			  end
			  %>
			  <%= ary.join('<li>⋯⋯</li>') %>
	  	</ul>

    <% end
end %>
		
      
    </section>
    <footer>
      <a href="mailto:yx.pu@live.com">yx.pu@live.com</a>
      <p>&copy; 抄襲自 Alexis Sellier</p>
    </footer>
  </body>
</html>

